import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
        marginBottom: 20,
    },
    currentPrice: {
        color: '#00E6D8',
        fontSize: 25,
        fontWeight: 'bold',
        paddingTop: 20,
    },
    textPrice: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
        paddingBottom: 20,
    }
})

export default styles